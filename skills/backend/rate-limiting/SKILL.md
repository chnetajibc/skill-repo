---
name: rate-limiting
description: "Rate limiting: per-user/IP/API-key quotas, cost caps, expensive-computation guards. Return 429 with clear contracts."
---

# rate-limiting

Rate limiting: per-user/IP/API-key quotas, cost caps, expensive-computation guards. Return 429 with clear contracts.
