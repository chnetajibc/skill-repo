---
name: domain-modeling
description: "Model entities, value objects, aggregates, ownership, invariants. Separate domain, persistence, DTO, response models; never auto-expose ORM entities."
---

# domain-modeling

Model entities, value objects, aggregates, ownership, invariants. Separate domain, persistence, DTO, response models; never auto-expose ORM entities.
